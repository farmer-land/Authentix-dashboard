# TRIAGE_QUEUE.md — Frontend

Regenerated by `tyrion-triage-scan` on 2026-08-19 22:11 IST, from [`FRONTEND_BATTLE_PLAN.md`](FRONTEND_BATTLE_PLAN.md).

## Top 2 priorities

### 1. Primary click targets are non-keyboard-operable (`<div onClick>`) — HIGH · XEN-69
**File:** `email-templates/page.tsx:487-494`, `broadcasts/page.tsx:739-786,1209-1229`
**Why now:** Explicit WCAG 2.1.1 Level A failure — the exact accessibility category this triage weights most heavily — on frequently-used controls (template cards, picker cards, contact-selection rows), not a peripheral surface.

### 2. Certificate design canvas is 100% pointer-only — HIGH · XEN-68
**File:** `generate-certificate/components/DraggableField.tsx:383-400`, `InfiniteCanvas.tsx:302,314,445-555`
**Why now:** Same WCAG 2.1.1 Level A keyboard-operability failure as #1, but on the single highest-traffic surface in the product — it blocks the entire generate-certificate workflow (the core value prop per Authentix-backend's CLAUDE.md) for keyboard-only users, not just one component within it.

---

Live Jira status is **not** cross-checked here. If you want the real current state, run a targeted JQL query, never an unfiltered scan:
```
project = XEN AND labels = team-frontend AND status != Done
```

Report-only — nothing has been dispatched automatically. Run `/code-review` or hand an item to `feature-builder`/`bug-fixer` manually to act on it.
