# TRIAGE_QUEUE.md — Frontend

> **Jira keys below are stale.** The `XEN` project was split on 2026-08-19 and has since been
> **deleted** — an `XEN-###` key resolves to nothing. Keys verified against each GARDEN ticket's own
> *Migrated from* line have been converted. Ones still shown as `XEN-##` could not be verified without
> guessing: the old numbering had gaps (`GARDEN-23` came from `XEN-92`, not `XEN-87`), so it does not
> convert by arithmetic. Find those by summary in `GARDEN` rather than trusting a computed key.

Regenerated by `tyrion-triage-scan` on 2026-08-20 09:56 IST, from `FRONTEND_BATTLE_PLAN.md` (generated 2026-08-19).

## Top 2

1. **[HIGH]** Hand-rolled modals skip Radix Dialog — no focus trap, no ARIA dialog semantics — worst instance in the **shared dashboard shell** — [src/components/dashboard/NotificationPanel.tsx:47-53](src/components/dashboard/NotificationPanel.tsx:47) (also `billing/page.tsx:1092-1206,164-172`) — Jira GARDEN-6
   Why now: `NotificationPanel` renders on every page, so this is the highest-traffic accessibility gap in the repo and the pattern other engineers are most likely to copy next.

2. **[HIGH]** Certificate design canvas is 100% pointer-only — fields can't be selected, moved, resized, or rotated via keyboard — [generate-certificate/components/DraggableField.tsx:383-400](generate-certificate/components/DraggableField.tsx:383) and `InfiniteCanvas.tsx:302,314,445-555` — Jira GARDEN-4
   Why now: blocks the entire generate-certificate workflow — the core product surface — for keyboard-only users; a WCAG 2.1.1 Level A failure on the primary feature, not a peripheral one.

Both keys above were verified against each ticket's own *Migrated from* line in Jira (GARDEN-6 ← XEN-70, GARDEN-4 ← XEN-68) and are current.

Live Jira status isn't cross-checked here — run `project = GARDEN AND status != Done` in Jira for the real current state before assuming either item is unclaimed.

Report-only — nothing has been dispatched automatically. Run /code-review or hand an item to feature-builder/bug-fixer manually to act on it.
